# Created by Agamdeep Singh / CodeWithAgam
# Youtube: CodeWithAgam
# Github: CodeWithAgam
# Instagram: @agamdeep_21, @coderagam001
# Twitter: @CoderAgam001
# Linkdin: Agamdeep Singh

from elements import elements

def main():
    print("Welcome to The Periodic Name App!")
    
    name = input("Enter you Name:\n").lower()

    selected_elements = []

    # A
    if name.count("ac") > 0:
        selected_elements.append("Actinium")

    if name.count("al") > 0:
        selected_elements.append("Aluminium")
    
    if name.count("am") > 0:
        selected_elements.append("Americium")
    
    if name.count("sb") > 0:
        selected_elements.append("Antimony")
    
    if name.count("ar") > 0:
        selected_elements.append("Argon")
    
    if name.count("as") > 0:
        selected_elements.append("Arsenic")
    
    if name.count("at") > 0:
        selected_elements.append("Astatine")

    # B
    if name.count("ba") > 0:
        selected_elements.append("Barium")

    if name.count("bk") > 0:
        selected_elements.append("Berkelium")

    if name.count("be") > 0:
        selected_elements.append("Beryllium")

    if name.count("bi") > 0:
        selected_elements.append("Bismuth")

    if name.count("bh") > 0:
        selected_elements.append("Bohrium")

    if name.count("b") > 0:
        selected_elements.append("Boron")

    if name.count("br") > 0:
        selected_elements.append("Bromine")

    # C
    if name.count("cd") > 0:
        selected_elements.append("Cadmium") 
    
    if name.count("ca") > 0:
        selected_elements.append("Calcium")

    if name.count("cf") > 0:
        selected_elements.append("Californium") 

    if name.count("c") > 0:
        selected_elements.append("Carbon")

    if name.count("ce") > 0:
        selected_elements.append("Cerium")

    if name.count("cs") > 0:
        selected_elements.append("Cesium")

    if name.count("cl") > 0:
        selected_elements.append("Chlorine")

    if name.count("cr") > 0:
        selected_elements.append("Chromium")
    
    if name.count("co") > 0:
        selected_elements.append("Cobalt")

    if name.count("cn") > 0:
        selected_elements.append("Copernicium")

    if name.count("cu") > 0:
        selected_elements.append("Copper")

    if name.count("cm") > 0:
        selected_elements.append("Curium")

    # D
    if name.count("ds") > 0:
        selected_elements.append("Darmstadtium")

    if name.count("db") > 0:
        selected_elements.append("Dubnium")

    if name.count("dy") > 0:
        selected_elements.append("Dysprosium")

    # E
    if name.count("es") > 0:
        selected_elements.append("Einsteinium")

    if name.count("er") > 0:
        selected_elements.append("Erbium")

    if name.count("eu") > 0:
        selected_elements.append("Europium")

    # F
    if name.count("fm") > 0:
        selected_elements.append("Fermium")

    if name.count("fl") > 0:
        selected_elements.append("Flerovium")

    if name.count("f") > 0:
        selected_elements.append("Fluorine")

    if name.count("fr") > 0:
        selected_elements.append("Francium")

    # G
    if name.count("gd") > 0:
        selected_elements.append("Gadolinium")

    if name.count("ga") > 0:
        selected_elements.append("Gallium")

    if name.count("ge") > 0:
        selected_elements.append("Germanium")
    
    if name.count("au") > 0:
        selected_elements.append("Gold")

    # H
    if name.count("hf") > 0:
        selected_elements.append("Hafnium")

    if name.count("hs") > 0:
        selected_elements.append("Hassium")

    if name.count("he") > 0:
        selected_elements.append("Helium")

    if name.count("ho") > 0:
        selected_elements.append("Holmium")

    if name.count("h") > 0:
        selected_elements.append("Hydrogen")

    # I
    if name.count("in") > 0:
        selected_elements.append("Indium")

    if name.count("i") > 0:
        selected_elements.append("Iodine")

    if name.count("ir") > 0:
        selected_elements.append("Iridium")
    
    if name.count("fe") > 0:
        selected_elements.append("Iron")

    # K
    if name.count("kr") > 0:
        selected_elements.append("Krypton")

    # L
    if name.count("la") > 0:
        selected_elements.append("Lanthanum")

    if name.count("lr") > 0:
        selected_elements.append("Lawrencium")

    if name.count("pb") > 0:
        selected_elements.append("Lead")

    if name.count("li") > 0:
        selected_elements.append("Lithium")

    if name.count("lv") > 0:
        selected_elements.append("Livermorium")

    if name.count("lu") > 0:
        selected_elements.append("Lutetium")

    # M
    if name.count("mg") > 0:
        selected_elements.append("Magnesium")

    if name.count("mn") > 0:
        selected_elements.append("Manganese")

    if name.count("mt") > 0:
        selected_elements.append("Meitnerium")

    if name.count("md") > 0:
        selected_elements.append("Mendelevium")

    if name.count("hg") > 0:
        selected_elements.append("Mercury")

    if name.count("mo") > 0:
        selected_elements.append("Molybdenum")

    if name.count("mc") > 0:
        selected_elements.append("Moscovium")

    # N
    if name.count("nd") > 0:
        selected_elements.append("Neodymium")

    if name.count("ne") > 0:
        selected_elements.append("Neon")

    if name.count("np") > 0:
        selected_elements.append("Neptunium")

    if name.count("ni") > 0:
        selected_elements.append("Nickel")

    if name.count("nh") > 0:
        selected_elements.append("Nihonium")

    if name.count("nb") > 0:
        selected_elements.append("Niobium")

    if name.count("n") > 0:
        selected_elements.append("Nitrogen")

    if name.count("no") > 0:
        selected_elements.append("Nobelium")

    # O
    if name.count("og") > 0:
        selected_elements.append("Oganesson")

    if name.count("os") > 0:
        selected_elements.append("Osmium")

    if name.count("o") > 0:
        selected_elements.append("Oxygen")

    # P
    if name.count("pd") > 0:
        selected_elements.append("Palladium")

    if name.count("p") > 0:
        selected_elements.append("Phosphorus")

    if name.count("pt") > 0:
        selected_elements.append("Platinum")

    if name.count("pu") > 0:
        selected_elements.append("Plutonium")

    if name.count("po") > 0:
        selected_elements.append("Polonium")

    if name.count("k") > 0:
        selected_elements.append("Potassium")

    if name.count("pr") > 0:
        selected_elements.append("Praseodymium")

    if name.count("pm") > 0:
        selected_elements.append("Promethium")

    if name.count("pa") > 0:
        selected_elements.append("Protactinium")

    # R
    if name.count("ra") > 0:
        selected_elements.append("Radium")

    if name.count("rn") > 0:
        selected_elements.append("Radon")

    if name.count("re") > 0:
        selected_elements.append("Rhenium")

    if name.count("rh") > 0:
        selected_elements.append("Rhodium")

    if name.count("rg") > 0:
        selected_elements.append("Roentgenium")

    if name.count("rb") > 0:
        selected_elements.append("Rubidium")

    if name.count("ru") > 0:
        selected_elements.append("Ruthenium")

    if name.count("rf") > 0:
        selected_elements.append("Rutherfordium")

    # S
    if name.count("sm") > 0:
        selected_elements.append("Samarium")

    if name.count("sc") > 0:
        selected_elements.append("Scandium")

    if name.count("sg") > 0:
        selected_elements.append("Seaborgium")

    if name.count("se") > 0:
        selected_elements.append("Selenium")

    if name.count("si") > 0:
        selected_elements.append("Silicon")

    if name.count("ag") > 0:
        selected_elements.append("Silver")

    if name.count("na") > 0:
        selected_elements.append("Sodium")

    if name.count("sr") > 0:
        selected_elements.append("Strontium")

    if name.count("s") > 0:
        selected_elements.append("Sulfur")

    # T
    if name.count("ta") > 0:
        selected_elements.append("Tantalum")

    if name.count("tc") > 0:
        selected_elements.append("Technetium")

    if name.count("te") > 0:
        selected_elements.append("Tellurium")

    if name.count("ts") > 0:
        selected_elements.append("Tennessine")

    if name.count("tb") > 0:
        selected_elements.append("Terbium")

    if name.count("tl") > 0:
        selected_elements.append("Thallium")

    if name.count("th") > 0:
        selected_elements.append("Thorium")
    
    if name.count("tm") > 0:
        selected_elements.append("Thulium")

    if name.count("sn") > 0:
        selected_elements.append("Tin")

    if name.count("ti") > 0:
        selected_elements.append("Titanium")

    if name.count("w") > 0:
        selected_elements.append("Tungsten")

    # U
    if name.count("u") > 0:
        selected_elements.append("Uranium")

    # V
    if name.count("v") > 0:
        selected_elements.append("Vanadium")

    # X
    if name.count("xe") > 0:
        selected_elements.append("Xenon")

    # Y
    if name.count("yb") > 0:
        selected_elements.append("Ytterbium")

    if name.count("y") > 0:
        selected_elements.append("Yttrium")

    # Z
    if name.count("za") > 0:
        selected_elements.append("Zaratite")

    if name.count("zr") > 0:
        selected_elements.append("Zirconium")

    if name.count("zn") > 0:
        selected_elements.append("Zinc")